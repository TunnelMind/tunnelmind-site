# recon/FLASH-BRIEF.md — Newcomer legibility + proposed signature hero (Phase 1 check 9 → Phase 3)

## Newcomer 5-second test (current state)
Open `/` cold, knowing nothing. You see, in order: eyebrow "TunnelMind · Trust Attestation Layer" → h1 **"The internet has no memory. We're building one."** → one-paragraph what-it-is → ipinfo-contrast one-liner → **"One corpus · Four lenses"** strip (Scry/Sigil/Tracker/GhostRoute, each with a blurb) → **VerifyWidget** (live `/v1/verify`) → live attacker-IP stats → `curl …/v1/check/{ip}` → all over a live 3D radar.

**Verdict:** legibility is **largely already there** — better than the §1 brief assumes. A newcomer *can* state "it's a trust/attestation graph for the agentic internet, four lenses, with a live check" in ~5s. The real gap is **not explanation — it's a single signature visual moment** that makes the *join* (the moat) felt, not just stated.

## The constraint that decides the concept
§7 wants "more flash" **and** "to the point" **and** "no new pages." §2's suggested flash device — the **Golden Spiral sacred-geometry tier mapping** — belongs to the **P-GEO design that was reverted 2026-06-04** (see STALENESS H1 / OPEN-QUESTIONS Q1). Using it re-introduces killed design. So the hero must be built in the **current** system: slate-navy `#0f172a`, Crimson Pro display, JetBrains Mono data, cyan/green/purple lens accents — and obey the surviving discipline **"every visual sits adjacent to hard, legible monospace data."**

## Proposed signature moment (one, no new page)
**Promote the existing `VerifyWidget` into THE hero event: "the four lenses converge on one verdict, live."**
1. Newcomer types/keeps a domain (default `api.anthropic.com`). On submit, the four lens nodes (Scry/Sigil/Tracker/GhostRoute) **light up in sequence and draw edges into a single fused verdict node** — the *join* animated, in lens colors, over the radar. This is the flash, and it is literally the moat.
2. Adjacent, in JetBrains Mono: the real `/v1/verify` response — per-lens blocks, the fused verdict, the **attestation tier** (`self-asserted → … → silicon-root`), the 5-min `sigil_token`, and the **`curl` equivalent**. Flash never floats alone; the data is right there.
3. Respect `prefers-reduced-motion` (static converged diagram fallback).
4. Net page count unchanged — this enhances `Radar.jsx`'s existing hero, adds nothing.

## chat.tunnelmind.ai (if scope expands — Q3)
§2 wants it reading as a **live graph terminal** (live API call, all lenses, receipt tier + curl), not a static FAQ. Cannot assess/modify — no source repo found locally. Flagged, deferred.

## What I will NOT do
Re-introduce Cinzel/cosmic/sacred-geometry/golden-spiral unless Q1 explicitly reverses the 2026-06-04 revert. Add no new top-level pages. Restyle nothing outside the hero without approval.
