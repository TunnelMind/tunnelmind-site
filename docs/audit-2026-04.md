# TunnelMind.ai Codebase Audit — April 2026

Pre-split audit. No code changes were made during this process.

> **Updated 2026-04-19** — Re-audited against current source. The site has been significantly
> updated since the initial audit: `TopNav`/`Footer` are already in `components/shared/`,
> `Landing.jsx` is the default route, and 13 routes are live. Sections below reflect current state.

---

## 1. Repo Structure

**Top-level layout:**
```
tunnelmind-site/
├── dist/                  # Vite build output
├── functions/             # Cloudflare Pages Functions (Stripe API handlers)
│   ├── _middleware.js     # CORS headers for /api/* routes
│   └── api/stripe/        # connect/create-account, connect/onboarding-link, payout, webhooks
├── public/                # Static assets (favicon.svg, trackers.json)
├── src/                   # React app source
│   ├── main.jsx           # React root mount point
│   ├── App.jsx            # Hash router + page dispatcher
│   ├── index.css          # CSS design token variables (full design system)
│   ├── lib/               # Shared utilities and state
│   │   ├── state.jsx      # Global state (useReducer + localStorage + Supabase sync)
│   │   ├── supabase.js    # Supabase client init (null in Phase 1)
│   │   ├── auth.js        # Email/password + GitHub OAuth wrappers
│   │   ├── fingerprint.js # Anonymous SHA-256 client fingerprinting
│   │   ├── proofOfWork.js # SHA-256 PoW challenge solver (stubbed in Phase 1)
│   │   └── scoring.js     # Score weights, identity tier multipliers, revenue waterfall
│   ├── pages/             # Top-level page components (one per route)
│   │   ├── Landing.jsx    # DEFAULT — product hero, tool cards, stats
│   │   ├── Tools.jsx      # Tool grid (NetProbe, Receipt, Radar)
│   │   ├── Api.jsx        # Tracker Data API + GhostRoute endpoint docs
│   │   ├── ReCenter.jsx   # WireGuard enrollment, eBPF, waitlist
│   │   ├── Whitepapers.jsx # ATAP paper (coming soon)
│   │   ├── About.jsx      # Mission, company info, TAO
│   │   ├── Pricing.jsx    # API tiers (Free/Pro/Team) + Personal tier
│   │   ├── Extension.jsx  # Extension install, changelog
│   │   ├── Receipt.jsx    # Surveillance Receipt generator (fully local)
│   │   ├── Products.jsx   # Alt product grid (legacy)
│   │   ├── Dialog.jsx     # Shadow Graph writing space (legacy — accessible by hash)
│   │   ├── Roadmap.jsx    # Roadmap as published doc (legacy)
│   │   └── Contributors.jsx # Leaderboard + Stripe Connect (legacy)
│   └── components/        # UI components
│       ├── shared/
│       │   ├── TopNav.jsx        # ✅ ALREADY BUILT — site-switcher-aware top nav
│       │   └── Footer.jsx        # ✅ ALREADY BUILT — shared footer
│       ├── DocumentEditor.jsx    # Editable document with publish toggle
│       ├── InteractiveSentence.jsx # Sentence-level voting + corrections + annotations
│       ├── AnnotationPanel.jsx   # Right-side threaded annotation panel
│       ├── CorrectionThread.jsx  # Sorted correction proposals with voting
│       ├── RedactionBlock.jsx    # Vote-to-declassify redacted text (5-vote threshold)
│       ├── WPChrome.jsx          # Ruler + status bar (WordPress-style chrome)
│       ├── PageDesc.jsx          # Page title + description header
│       ├── AuthModal.jsx         # Email/password + GitHub OAuth login modal
│       ├── StripeConnect.jsx     # 4-state payout management dashboard
│       └── IntroBox.jsx          # First-visit onboarding modal
├── supabase/              # Schema migrations (SQL files)
├── index.html             # Entry point
├── vite.config.js         # Vite build config
└── wrangler.toml          # Cloudflare Pages config
```

**Supabase client location:** `src/lib/supabase.js` — exports a client initialized from `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY`. Returns `null` when env vars are unset (Phase 1 mode).

**Shadow Graph code location:** Distributed across `src/components/` (InteractiveSentence, AnnotationPanel, CorrectionThread, RedactionBlock) and `src/pages/Dialog.jsx`. State is in `src/lib/state.jsx`.

---

## 2. Current Routes

The app uses a **hash-based router** (`/#/path`). All routing is handled client-side in `App.jsx`; no server-side routing rules exist. **No `t/` prefix in URLs** — it was cosmetic display text that has already been cleaned up.

| Hash Route | Page Component | Description |
|---|---|---|
| `/#/` (default) | `Landing.jsx` | Product hero, tool cards, stats strip |
| `/#/tools` | `Tools.jsx` | Tool grid (NetProbe, Receipt, Radar) |
| `/#/api` | `Api.jsx` | Tracker Data API docs + GhostRoute endpoint |
| `/#/recenter` | `ReCenter.jsx` | WireGuard enrollment, eBPF, waitlist |
| `/#/whitepapers` | `Whitepapers.jsx` | ATAP whitepaper (coming soon) |
| `/#/about` | `About.jsx` | Mission, company info, TAO |
| `/#/pricing` | `Pricing.jsx` | API tiers (Free/Pro/Team) + Personal tier |
| `/#/extension` | `Extension.jsx` | Extension install, changelog |
| `/#/receipt` | `Receipt.jsx` | Surveillance Receipt tool (fully local) |
| `/#/products` | `Products.jsx` | Alt product grid (legacy) |
| `/#/dialog` | `Dialog.jsx` | Shadow Graph writing space (legacy — flagged shadowGraphPages) |
| `/#/roadmap` | `Roadmap.jsx` | Roadmap as published doc (legacy — flagged shadowGraphPages) |
| `/#/contributors` | `Contributors.jsx` | Leaderboard + Stripe Connect (legacy — flagged shadowGraphPages) |

**`shadowGraphPages`** set in `App.jsx`: `{dialog, roadmap, contributors, about}` — these suppress the footer and render `StatusBar` instead.

---

## 3. Component Inventory

### DESIGN_SYSTEM
| Component / File | Role |
|---|---|
| `src/index.css` | CSS custom properties — full color palette, typography scale, spacing, animation keyframes |
| `src/components/shared/TopNav.jsx` | ✅ **Already built** — top nav with `site` prop (`"tunnelmind"` or `"alloy"`), site switcher, responsive mobile menu |
| `src/components/shared/Footer.jsx` | ✅ **Already built** — shared footer with legal links, social, copyright |
| `src/components/WPChrome.jsx` | Ruler (tick-mark ruler) + StatusBar (live vote/annotation counts) + MenuBar stub |
| `src/components/PageDesc.jsx` | Page-level title + description header |
| `src/components/AuthModal.jsx` | Email/password signup + sign-in + GitHub OAuth modal |
| `src/components/IntroBox.jsx` | First-visit onboarding checklist modal |
| `src/lib/fingerprint.js` | Anonymous client fingerprinting (UA + language + timezone + screen → SHA-256) |
| `src/lib/proofOfWork.js` | SHA-256 PoW challenge solver (Phase 3 — currently always returns valid) |

### PRODUCT_SPECIFIC
| Component / File | Role |
|---|---|
| `src/pages/Products.jsx` | Tool catalog — hardcoded `LIVE_TOOLS` + `PERSONAL_TOOLS` arrays; links to external tools |
| `src/pages/Roadmap.jsx` | Roadmap as published document (DocumentEditor in read mode) |
| `src/pages/About.jsx` | Origin story as published document |
| `src/pages/Receipt.jsx` | Surveillance Receipt — fully client-side, reads `public/trackers.json` |
| `src/components/DocumentEditor.jsx` | Editable document with author/publish toggle (used on Dialog, Products, Roadmap, Contributors, About) |
| `src/components/StripeConnect.jsx` | 4-state payout UI: not-setup → onboarding → eligible → deauthorized |
| `src/pages/Contributors.jsx` | Leaderboard + compensation framework description + Stripe Connect integration |

### SHADOW_GRAPH
| Component / File | Role |
|---|---|
| `src/pages/Dialog.jsx` | Main writing space — renders DocumentEditor with all Shadow Graph features enabled |
| `src/components/InteractiveSentence.jsx` | **Core Shadow Graph unit** — sentence-level up/down voting, inline correction form, annotation trigger |
| `src/components/AnnotationPanel.jsx` | Right-side panel — threaded annotations with voting + replies |
| `src/components/CorrectionThread.jsx` | Sorted correction proposals with voting; author apply button |
| `src/components/RedactionBlock.jsx` | Vote-to-declassify UI — progress bar toward threshold (default: 5 votes) |
| `src/lib/state.jsx` (partial) | All reducer actions for votes, corrections, annotations, redaction |
| `src/lib/scoring.js` | `SCORE_WEIGHTS`, identity tier multipliers, `REVENUE_WATERFALL` constants |

### OTHER
| Component / File | Role |
|---|---|
| `src/App.jsx` | Router logic + page dispatcher |
| `src/main.jsx` | React root mount |
| `src/lib/state.jsx` | Global `useReducer` state, localStorage persistence, Supabase sync (Phase 2) |
| `src/lib/supabase.js` | Supabase client initialization |
| `src/lib/auth.js` | Auth wrappers: email/password, GitHub OAuth, session management |

---

## 4. Supabase Tables

**Current sync status:** Most tables are defined in migrations but not yet actively synced. Only `contributors` (read) and `contribution_ledger` (write) are wired in Phase 1.

| Table | Purpose | Key Columns | Active Sync |
|---|---|---|---|
| `transmissions` | Document pages | id, page, section_key, title, created_at | No (seeded) |
| `sentences` | Individual sentences | id, transmission_id, sentence_key, content, is_redacted, redaction_threshold | No |
| `sentence_votes` | Sentence up/down votes | id, sentence_id, fingerprint, direction (-1/1) | No |
| `corrections` | Proposed text corrections | id, sentence_id, proposed_text, contributor_fingerprint, is_applied | No |
| `correction_votes` | Votes on corrections | id, correction_id, fingerprint, direction | No |
| `annotations` | Threaded comments | id, sentence_id, parent_id, content, contributor_fingerprint, is_author | No |
| `annotation_votes` | Votes on annotations | id, annotation_id, fingerprint, direction | No |
| `redaction_votes` | Declassification votes | id, sentence_id, fingerprint, created_at | No |
| `contributors` | User profiles + payout | id, handle, fingerprint, email, email_verified, stripe_* fields, identity_tier | **Yes (read)** — `state.jsx` loads Stripe status on auth |
| `contribution_ledger` | Append-only scoring log | id, contributor_id, contributor_fingerprint, action_type, target_id, points, created_at | **Yes (write)** — `state.jsx` inserts when `points > 0` and authenticated |
| `score_weights` | Configurable action weights | action_type (PK), weight, updated_at | No (hardcoded in scoring.js) |
| `payout_ledger` | Monthly credits/debits | id, contributor_id, amount_cents, reason, payout_run_id, created_at | No |
| `payout_runs` | Monthly payout cycles | id, period (PK), status, total_revenue_cents, business_reserve_cents, contributor_pool_cents | No |

**Views:**
- `sentence_net_scores` — sum of votes per sentence
- `correction_net_scores` — sum of votes per correction
- `redaction_progress` — declassification progress per sentence
- `contributor_scores` — materialized, total weighted points per contributor

---

## 5. Environment Variables

| Variable | Used In | Purpose |
|---|---|---|
| `VITE_SUPABASE_URL` | `src/lib/supabase.js` | Supabase project URL; absence disables Supabase |
| `VITE_SUPABASE_ANON_KEY` | `src/lib/supabase.js` | Supabase anon key |
| `VITE_TURNSTILE_SITE_KEY` | `wrangler.toml` (referenced but not yet consumed in React code) | Cloudflare Turnstile bot protection |
| `VITE_AUTHOR_KEY` | `src/components/SubredditNav.jsx` (defined, never read) | Unused — remove |
| `STRIPE_SECRET_KEY` | Cloudflare Functions (`functions/api/stripe/*`) | Stripe API secret (server-side only, in CF dashboard) |
| `STRIPE_WEBHOOK_SECRET` | `functions/api/stripe/webhooks.js` | Stripe webhook signature verification |

**CF Pages build command:** `npx vite build`
**CF Pages deploy command:** `npx wrangler pages deploy dist --project-name tunnelmind-site`

---

## 6. Cloudflare Pages Configuration

**`wrangler.toml`:**
```toml
name = "tunnelmind-site"
compatibility_date = "2024-01-01"
```
Minimal config — no route rules, no KV, no R2, no Workers bindings.

**`functions/_middleware.js`:**
- Injects CORS headers on all `/api/*` responses
- Allowed origins: `https://tunnelmind.ai`, `http://localhost:5173`
- **Split concern:** `alloy.tunnelmind.ai` must be added to the allowed origins list if it calls these endpoints, OR Alloy must have its own Functions.

**Cloudflare Pages Functions (in `/functions/api/stripe/`):**
| Endpoint | Method | Purpose |
|---|---|---|
| `connect/create-account.js` | POST | Create Stripe Express Connect account |
| `connect/onboarding-link.js` | POST | Return Stripe hosted onboarding URL |
| `payout.js` | POST | Trigger payout (implementation TBD) |
| `webhooks.js` | POST | Handle Stripe webhook events (account.updated, etc.) |

**No `_redirects` or `_headers` files** — the hash router handles all client-side navigation. No server-side routing dependencies.

---

## 7. Dependencies of Concern

**`package.json` runtime dependencies:**
```json
{
  "@supabase/supabase-js": "^2.103.0",
  "react": "^18.3.1",
  "react-dom": "^18.3.1",
  "stripe": "^22.0.1"
}
```

**Split risk assessment:**

| Risk Item | Severity | Detail |
|---|---|---|
| `localStorage` key collision | Medium | Both sites would share `tm_state` key if opened in same browser. Needs namespacing (`tm_state` vs `alloy_state`). |
| Supabase schema coupling | Medium | All tables in one schema. Alloy needs a separate schema or project. `contribution_ledger` must remain unified for global scoring. |
| Product data hardcoded | Medium | `Products.jsx` has hardcoded `LIVE_TOOLS` / `PERSONAL_TOOLS` arrays. Extract to API or shared package before split. |
| Seeded documents hardcoded | Low | `SEEDED_DOCUMENTS` in `state.jsx` are site-specific. Each site needs its own seeds. |
| Stripe account | Low | Payout account is unified — desired behavior. Both sites can share the same Stripe account via unified Supabase `contribution_ledger`. |
| CORS allowlist | Low | `functions/_middleware.js` will need `alloy.tunnelmind.ai` added. |
| `VITE_AUTHOR_KEY` unused | Trivial | Delete from env and code. |
| Hash router | No risk | Zero server-side dependency — clean split. |
| No Redux or heavy state | No risk | `useReducer` + context is easily duplicated or extracted. |
| No CDN dependencies | No risk | No external CDN scripts in `index.html`. |

---

## 8. Shadow Graph Separability Assessment

The Shadow Graph is **clearly separable.** All Shadow Graph code is isolated to:

- `src/pages/Dialog.jsx` — the host page
- `src/components/InteractiveSentence.jsx`
- `src/components/AnnotationPanel.jsx`
- `src/components/CorrectionThread.jsx`
- `src/components/RedactionBlock.jsx`
- State slices in `src/lib/state.jsx`: `sentenceVotes`, `corrections`, `annotations`, `redactionVotes`, `appliedTexts`
- Supabase tables: `sentences`, `sentence_votes`, `corrections`, `correction_votes`, `annotations`, `annotation_votes`, `redaction_votes`, `transmissions` (and views)

**None of these are imported by the product pages** (Products, Roadmap, About, Receipt). The `DocumentEditor.jsx` component hosts Shadow Graph via props — when Shadow Graph features are disabled, it renders as a plain document viewer.

The only shared dependency is `src/lib/scoring.js` (needed by both sites for contribution weighting) and `contribution_ledger` (the unified scoring table).

---

## 9. Scoring Model (Canonical)

**Score weights** (locked before Stripe goes live — in `src/lib/scoring.js`):
| Action | Weight |
|---|---|
| `annotation_created` | 5 |
| `vote_cast` | 1 |
| `upvote_received` | 2 |
| `correction_accepted` | 10 |
| `correction_proposed` | 0 |

**Identity tier multipliers (applied at payout time):**
| Tier | Multiplier | Requirement |
|---|---|---|
| `email` | 0.5× | Email verified |
| `verified` | 1.0× | GitHub OAuth |
| `atap` | 1.5× | TunnelMind desktop + hardware attestation |

**Revenue waterfall:**
- 15% business reserve
- 85% contributor pool
- Monthly payout cycle, $10 minimum threshold, sub-threshold balances carry forward

---

## 10. Pre-Split Decisions Required

Before Phase 1 coding begins, the following decisions must be made:

1. **Supabase:** Separate project for Alloy, or separate schema in the same project?
   - Recommendation: Same project, separate schemas (`public` for tunnelmind, `alloy` schema for alloy). Unified auth realm, shared `contribution_ledger`, Stripe account stays unified.

2. **Repo structure for Alloy:** New standalone repo, or second workspace in a monorepo?
   - See Phase 2 prompt for recommendation request.

3. **Shared design token package:** Shared npm package, shared git submodule, or symlinked folder?
   - Recommendation: Shared folder in a monorepo with path alias in Vite configs.

4. **`localStorage` namespacing:** Confirm `tm_state` stays for tunnelmind.ai and Alloy uses `alloy_state`.

5. **Stripe:** Confirm unified Stripe account. Alloy contributors earn in the same pool.

6. **CORS:** Confirm Alloy can call `functions/api/stripe/*` on tunnelmind.ai, or Alloy needs its own Stripe Functions.

---

*Audit produced: 2026-04-18. No code changes made.*
