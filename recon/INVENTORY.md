# recon/INVENTORY.md — Site surface inventory (Phase 0, read-only)

> Generated 2026-06-21. SITE_REPO = `/home/o2k/tunnelmind-site`. VAULT consulted as primary truth.
> Nothing mutated. Build artifacts (`dist/`, `node_modules/`, `graphify-out/`) excluded.

## 1. Architecture (how the site is built)
- **Vite + React SPA**, history-router (clean URLs since 2026-05-31, `#47`). `src/App.jsx` maps a page key → component; old `#/x` hash links are redirected on load.
- **Build pipeline** (`package.json`): generates static standards HTML (`build-oai/atap/eat/receipt/verdict/standards`), the OG image, then `vite build`, then SEO route stubs (`build-route-stubs.js`).
- **CF Pages** (`wrangler.toml`), `pages_build_output_dir = dist`. API reached over plain HTTPS to `api.`/`data.tunnelmind.ai` (no service binding). Rate-limiter is a separate Worker (`tunnelmind-ratelimiter`).
- **Design tokens** in `src/index.css :root`. Fonts self-hosted via `@fontsource`: **Cinzel + Crimson Pro + JetBrains Mono**. Palette: slate-navy `#0f172a`. CSS comments explicitly note a **revert from P-GEO** ("reverted from P-GEO 'Cinzel'", "reverted from P-GEO cosmic near-black").
- No repo `CLAUDE.md`. `graphify-out/` present (code graph).

## 2. SPA routes (`src/pages/`) — 17 total
| Route | Component | Purpose | Notes |
|---|---|---|---|
| `/` (landing) | `Radar.jsx` | **Homepage** = live 3D attacker-corpus radar + hero + four-lens strip + VerifyWidget + live curl | carries the legibility/flash burden |
| `/vision` | `Landing.jsx` | Long-form manifesto/essay (distinct from `/`) | keep — not a dup |
| `/products` | `Products.jsx` | Product registry (Corpus / Surfaces / Standards / Verification) | Tracker lens missing |
| `/api` | `Api.jsx` | REST + MCP endpoint catalog | |
| `/tools` | `Tools.jsx` | Tooling surface | |
| `/skills` | `Skills.jsx` | Claude Skills distribution | not in sitemap |
| `/compare` | `Compare.jsx` | vs ipinfo/GreyNoise; **live `/v1/stats`** w/ fallbacks | not in sitemap |
| `/pricing` | `Pricing.jsx` | $20 Stripe blocks (humans) + x402 (agents) | hash-link + checkout-redirect issues |
| `/roadmap` | `Roadmap.jsx` | H1/H2/H3 gated roadmap | correctly notes Defender retired |
| `/whitepapers` | `Whitepapers.jsx` | thin; overlaps `/standards` | ATAP name wrong |
| `/about` | `About.jsx` | company/corpus/"what we don't do" | |
| `/privacy` `/terms` `/law-enforcement` `/abuse` `/transparency` `/account-risk` | `legal/*` | 6 legal pages | Terms describes killed tiers; Privacy mentions "the VPN" |

## 3. Static published pages (`public/**/*.html`) — 9
`standards.html`, `standards/receipt-format/v1.html`, `standards/reconciliation-verdict/v1.html`, `standards/compliance-ledger/v1.html`, `oai/standard.html`, `oai/index.html`, `atap/standard.html`, `atap/profiles.html`, `eat/profile/v0.1.html`. Generated by `scripts/build-*-page.js` (edit generators, never generated HTML — `reference_tunnelmind_site_generated_html`).

## 4. Agent / discovery surface (`public/`)
`llms.txt`, `agent-manifest.json`, `agent-onboarding.md`, `robots.txt`, `sitemap.xml`, `.well-known/{ai-services,atap,mcp,x402,receipt-signing-key,receipt-revocations}.json`, `atap/` schemas + context, OG image.

## 5. White papers / long-form docs (`docs/`, repo-only — NOT served to `dist/`)
Published specs: `ATAP-v0.1.md`, `OAI-STANDARD-v1.md`, `EAT-PROFILE-v0.1.md`, receipt/verdict/compliance MDs (these are the *sources* the build turns into the public HTML). Decision records: `docs/decisions/*`, `docs/ADR-*`. **Relic:** `docs/audit-2026-04.md` (pre-pivot architecture: Shadow Graph, Alloy, Stripe Connect, payout, contribution_ledger).

## 6. Sibling surfaces (read-only, NOT in mutation scope unless expanded)
- `tunnelmind-radar` (`radar.git`) — standalone 3D radar; `radar.*` is 301'd to `tunnelmind.ai`. Killed-concept scan: **clean**. Lower priority.
- `chat.tunnelmind.ai`, `mcp*.tunnelmind.ai` — referenced as in-scope by §2/§4 but **no dedicated source repo found in `/home/o2k`** (Workers live in data-api/scry-server). Deferred — see OPEN-QUESTIONS.

## 7. Live-vs-repo (deployment) snapshot (three-way seed)
- LIVE `index.html` title/meta = "Trust Attestation" ✅ matches canonical framing.
- LIVE `llms.txt` = "**observability layer**" ❌ stale (contradicts the live index).
- LIVE `/v1/stats` (2026-06-20): publishers 10,003 · ssps 2,657 · dsps 13,241 · **sell_paths 1,787,537** · **exchange_seats 970,759** · tracker entities 59,212 / domains 62,022 · ghostroute ai_infra 49 · oai_registry 201.

## Phase 4 — brand visual assets (added 2026-06-21)
- `public/assets/how-it-works.{svg,png}` — customer-facing product explainer.
- `public/assets/the-moat.{svg,png}` — investor-facing thesis/moat frame.
- Generated by `scripts/build-brand-assets.js` (`npm run build:assets`, wired into `npm run build`). Provenance: `recon/ASSETS.md`.
