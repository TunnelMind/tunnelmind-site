{
  "name": "tunnelmind-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:oai && npm run build:atap && npm run build:eat && npm run build:receipt && npm run build:verdict && npm run build:standards && npm run build:og && npm run build:assets && vite build && npm run build:routes",
    "build:oai": "node scripts/build-oai-page.js",
    "build:atap": "node scripts/build-atap-page.js",
    "build:eat": "node scripts/build-eat-page.js",
    "build:receipt": "node scripts/build-receipt-page.js",
    "build:verdict": "node scripts/build-verdict-page.js",
    "build:standards": "node scripts/build-standards-index.js",
    "build:og": "node scripts/build-og-image.js",
    "build:assets": "node scripts/build-brand-assets.js",
    "build:routes": "node scripts/build-route-stubs.js",
    "preview": "vite preview",
    "test": "vitest run",
    "seed:oai": "node scripts/generate-oai-seed.js"
  },
  "dependencies": {
    "@fontsource/crimson-pro": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "@supabase/supabase-js": "^2.103.0",
    "3d-force-graph": "^1.80.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "stripe": "^22.0.1",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "marked": "^18.0.3",
    "sharp": "^0.34.5",
    "vite": "^5.4.11",
    "vitest": "^4.1.6"
  }
}
